export interface IPRODUCT {
  id: string;
  name: string;
  image: string;
  price: number;
}

export interface INAV {
  id: string;
  to: string;
  text: string;
}

export interface USER {
  name: string;
  email: string;
}
